# Todo

## 目标

- 优化 "文档检索" 页面交互与信息表达
- 让普通用户看得懂检索规则、检索范围与返回结果
- 增强结果展示：更多结果、可读摘要、关键词高亮、点击查看原文详情

## 当前现状

- 当前检索页只有输入框、返回数量和结果表格
- 默认返回数量较少，当前 UI 默认 5，后端默认 5
- 当前实现是混合检索：全文检索优先，未命中时有 LIKE 兜底，并结合向量检索与 rerank
- 当前 UI 没有把“支持什么查询方式、不支持什么方式”解释清楚
- 当前结果表格没有关键词高亮，也没有点击后查看原文详情的独立区域

## 待办

1. 增加检索功能说明
- 在 "文档检索" 页增加清晰说明区
- 明确说明支持关键词、短语、整句输入
- 明确说明当前不是严格精确匹配，属于混合召回
- 明确说明当前暂不支持正则表达式
- 评估是否支持多组关键词输入，并定义格式

2. 优化检索参数
- 默认返回数量改为 10
- 最大返回数量改为 100
- 前后端参数保持一致

3. 优化检索结果摘要
- 在结果摘要中说明本次检索方式与匹配特点
- 解释“更适合关键词/整句，不适合正则”的边界

4. 优化结果表格展示
- 让内容摘要支持折行
- 高亮检索关键词
- 补充文档名称、定位、来源、得分等更清晰展示

5. 增加结果详情区
- 点击检索结果后，显示原文详情面板
- 展示文档名称、片段 ID、定位、检索来源、得分、命中来源、原文内容
- 视情况补充前后文片段

6. 补测试
- 补 viewmodels 测试：说明文案、结果格式化、高亮逻辑、详情视图
- 补 UI 测试：默认数量、最大数量、详情模块存在、组件配置正确

7. 验证
- 运行相关测试
- 运行全量测试
- 重启本地 UI 并手工验收检索页

## 备注

- git commit / push 按你的规则需要再次明确确认后才执行