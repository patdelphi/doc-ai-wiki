# Todo

## 当前需求

- 在 `"文档管理"` 模块增加 `"入库质检"` 功能
- 用于验证文档入库后的章节、分块、索引和检索效果是否达标

## 目标

1. 单文档可检查
- 展示文档基础状态
- 展示章节数、分块数、平均分块长度
- 展示首尾章节标题
- 展示抽样分块预览

2. 索引完备性可检查
- 校验 `ingest_status / index_status / error_message`
- 校验是否存在章节为空、分块为空、分块过少、分块过多等异常
- 给出质检结论与风险提示

3. 检索效果可抽样验证
- 输入验证关键词或问题
- 返回该文档内的命中片段
- 用于判断分块与索引是否可用

4. 放在文档管理页
- 不新增独立菜单
- 作为 `"文档管理"` 中选中文档后的质检面板

## 实现步骤

1. 服务层
- 新增单文档入库质检方法
- 汇总章节数、分块数、平均长度、首尾章节、抽样分块
- 增加异常规则判断与结果说明

2. Repository
- 增加按 `doc_uid` 查询章节、分块统计与抽样数据的能力
- 所有查询增加异常处理

3. UI
- 在 `"文档管理"` 中增加 `"入库质检"` 按钮
- 增加质检结果面板
- 增加文档内检索验证输入框与结果区

4. 测试
- 先补 repository/service 单测
- 再补 viewmodel/UI 结构测试
- 最后做浏览器验收

## 验收标准

- 能对单篇文档输出结构化质检结果
- 能发现明显异常：无章节、无分块、索引失败、分块异常
- 能在文档内做最小检索验证
- 文档管理页操作链路清晰，不影响现有注册/重建流程

## 备注

- 优先基于当前 SQLite 与现有检索能力实现，不引入新存储
- 先做单文档质检，批量质检后续再补

## 第二阶段

1. 批量入库质检
- 增加全量已入库文档批量质检入口
- 输出每篇文档的章节数、分块数、全文索引数、向量数、质检等级与风险摘要

2. CSV 导出
- 支持导出批量质检结果
- 文件保存到 `"Docs"` 目录
- 编码为 UTF-8 并增加 BOM 头

3. 阈值可配置
- 将章节过少、分块过碎、超长分块、过短分块等阈值做成本地配置
- 先提供页面内可保存配置
- 保存后立即影响入库质检判断

## 第三阶段

1. AI 质检效果评测
- 在 `"AI 质检"` 页面增加 `"效果评测"` 区域
- 支持粘贴样例 JSON，批量执行已有质检流程
- 样例先支持：`case_id / input_text / expected_overall_verdict / expected_risk_level / expected_claim_count`

2. 评测结果汇总
- 输出样例总数、总体结论命中数、风险等级命中数、Claim 数命中数、完全命中数
- 能快速看出误报、漏报和分级偏差

3. 评测明细
- 逐条显示预期值、实际值、是否命中、输入摘要
- 便于人工回看和继续调模板

4. 测试
- 先补 `quality service` 单测
- 再补 `viewmodel` 展示测试
- 最后补 `UI` 结构测试

## 第四阶段

1. 全页面排版优化
- 继续优化 `"文档管理"`，统一重点区、结果区、操作区的层级和间距
- 优化 `"文档检索"` 页面，强化输入区、结果摘要、结果明细的主次关系
- 优化 `"AI 质检"` 页面，统一输入、模板、结果、评测区的视觉节奏
- 优化 `"人工审核"` 页面，突出待处理记录、详情、证据、提交动作
- 优化 `"功能设置"` 页面，统一说明区、模板列表、详情和表单的排版密度

2. 结果下载能力
- 所有“有内容产出”的功能都增加 `"下载结果"` 按钮
- 下载文件格式统一为 `.txt`
- 文件内容统一写入 Markdown 文本，便于查看、归档、二次复制
- 文件统一保存到 `"Docs"` 目录

3. 首批覆盖范围
- `"文档管理"`：入库质检单文档结果、批量质检结果、文档内检索验证结果
- `"文档检索"`：检索摘要、检索结果列表、当前选中结果详情
- `"AI 质检"`：质检摘要、Claim 明细、证据明细、效果评测结果
- `"人工审核"`：当前 Claim 详情、证据详情、审核结果、审核历史明细
- `"功能设置"`：当前模板详情、运行配置展示结果

4. 导出实现约束
- 导出内容统一由 viewmodel 生成 Markdown 文本，避免页面和下载内容不一致
- 文件名包含模块名、结果类型、时间戳
- 所有导出接口增加异常处理
- `.txt` 文件使用 UTF-8 编码

5. 实施顺序
- 第一步：先统一导出基础能力与 Markdown 生成函数
- 第二步：先完成 `"文档检索"` 和 `"AI 质检"` 下载能力
- 第三步：完成 `"文档管理"`、`"人工审核"`、`"功能设置"` 下载能力
- 第四步：统一全页面排版细节和样式
- 第五步：补充 UI / viewmodel / service 测试并做页面验收

6. 验收标准
- 每个页面都能明显看出主操作区、结果区、说明区
- 每个有内容产出的模块都能下载 `.txt`
- 下载文件打开后是结构化 Markdown 文本
- 页面展示内容与下载内容语义一致
- 现有功能链路不受影响

## 第五阶段

1. 核心质检逻辑增强
- 不再把质检核心建立在固定关键词规则上
- 以“知识库证据 + LLM 判断”为主，规则仅作为风险提示与保守约束
- 将 Claim 判断分为：支持、矛盾、证据不足 三类

2. 检索链路增强
- 针对带有唯一化、绝对化、全称化、否定化约束的 Claim，除了原句检索，还要执行放宽逻辑约束后的主题检索
- 目标是补召回例外、反证、边界条件，而不是只召回最相似的支持性片段

3. 判定链路增强
- 启发式层不再使用“有证据即 verified”的粗粒度逻辑
- 模型判定前先识别 Claim 的逻辑约束类型
- 模型输出需显式区分 `support / contradict / insufficient`
- 合并结果时采用更保守策略，防止模型把应复核或应驳回的内容误放行

4. 验收重点
- 类似“唯一/只有/所有/绝不”这类 Claim，若知识库存在反证，应判为 `rejected`
- 若只检索到近似支持但不足以覆盖其逻辑约束，应判为 `needs_review`
- 普通事实陈述在有直接证据时仍可保持 `verified`

## 第六阶段

1. 效果评测说明细化
- 解释“效果评测”不是再次检索，而是用样例集对比预期与实际输出
- 明确区分严格指标与宽松指标，避免用户误把 `完全命中` 当作唯一可用性指标
- 补充“为什么会出现 0 条完全命中”的常见原因说明

2. 效果评测结果细化
- 在摘要区增加更细的指标：`核心命中`、`宽松命中`、`未命中主因`
- 将 `结论命中 / 风险命中 / Claim 数命中 / 完全命中` 的判定口径直接展示在页面
- 增加命中率百分比，不只显示命中条数

3. 评测明细细化
- 明细表增加逐项对比列，而不是只看最终 `完全命中`
- 分别展示：`结论是否命中`、`风险是否命中`、`Claim 数是否命中`
- 增加“差异说明”列，指出是结论偏差、风险偏保守，还是 Claim 拆分不一致
- 视情况增加“建议排查方向”列，帮助定位是模板问题、检索问题还是拆分问题

4. 交互与展示优化
- 优化摘要卡片文案，让用户先看核心指标，再看严格指标
- 优化明细表列顺序，按“样例 -> 预期 -> 实际 -> 命中情况 -> 差异说明”排列
- 必要时增加颜色语义或标签，突出“主要失败原因”

5. 测试与验收
- 先补 `viewmodel` 单测，锁定新增指标和字段展示
- 再补 `UI` 结构测试，锁定新增说明面板与明细列
- 最后执行页面验收，确认用户能直接看懂为什么未命中

## 第七阶段

1. 全表格统一分页
- 盘点 `"文档管理"`、`"文档检索"`、`"AI 质检"`、`"人工审核"`、`"功能设置"` 中所有表格
- 所有表格默认单页最多显示 `10` 行
- 超过 `10` 行时统一通过分页组件切换，而不是一次性全部渲染

2. 全表格统一自然序号
- 每个表格第一列增加自然序号
- 序号按当前分页视图连续显示，便于用户定位和沟通
- 保留原有业务主键字段，不用序号替代真实 ID

3. 分页组件统一
- 每个表格增加上一页 / 下一页 / 当前页 / 总页数相关组件
- 需要时补充“每页最多 10 行”的提示
- 分页切换后保持当前表格上下文，不打乱详情联动

4. 交互与兼容
- 选择表格某一行后，翻页和返回时要尽量保持可理解状态
- 对已有“点击行查看详情”的表格，分页后仍能正常联动详情区
- 导出逻辑继续基于全量数据，不受分页显示限制

5. 测试与验收
- 先补公共分页辅助函数或 viewmodel 测试
- 再补 `UI` 结构测试，锁定序号列和分页组件
- 最后逐页验收表格展示、翻页和详情联动

## 第八阶段

1. AI 质检证据召回增强
- 继续提升 `"AI 质检"` 的证据覆盖率，不只召回最相似片段
- 在原有“原句检索 + 放宽逻辑约束检索”基础上，补充更多面向反证、例外、边界条件的召回策略
- 优先优化“证据不全面、反证漏召回、只找到局部支持证据”的问题

2. 检索与排序优化
- 复核 `fulltext / vector / rerank / neighbor_window / final_top_k` 的组合策略
- 适当扩大候选召回，再通过重排和去重收敛最终证据集
- 优化同文档相邻片段、同章节上下文和跨查询结果的合并逻辑，减少重复证据，提升互补证据比例

3. Claim 分析与查询构造增强
- 继续增强对唯一化、绝对化、全称化、否定化、比较型表述的识别
- 针对不同类型 Claim 生成更合适的检索查询，避免只围绕原句字面相似度查找
- 必要时增加“主题实体 + 关系词 + 约束词拆分”的查询构造方式

4. 模型判定与合并策略优化
- 强化 LLM 提示词，要求显式比较“支持证据 / 矛盾证据 / 缺失证据”
- 优化启发式与 LLM 合并策略，降低“局部支持即通过”的风险
- 让结果更容易区分：证据充分、证据冲突、证据覆盖不足、需人工复核

5. 评测与样例增强
- 增加更容易暴露漏召回问题的评测样例，重点覆盖反证、唯一化、范围限定、边界条件
- 对比优化前后 `结论命中 / 风险命中 / 宽松命中 / 完全命中`
- 优先通过效果评测和典型误判样例验证改动是否真的提升

6. 测试与验收
- 先补 `quality service` 单测，锁定新增召回和判定逻辑
- 再补 `viewmodel / UI` 必要断言，保证展示字段不回退
- 最后结合真实 Claim 做页面验收，重点看证据是否更全面、关系解释是否更可信

## 第九阶段

1. 版本号调整
- 将项目版本更新为 `0.5`
- 优先修改 `"pyproject.toml"` 中的版本字段
- 如代码或文档中存在显式版本展示，一并检查并同步

2. README 重写
- 基于当前实际能力重写 `"readme.md"`
- 使用中文完整描述：项目背景、建设目标、核心功能、目录结构、技术栈、配置说明、部署与启动方法、测试方式、当前能力边界
- 内容以当前代码和页面能力为准，不写未实现能力

3. 文档质量要求
- README 需适合作为对外项目主页说明
- 部署步骤要覆盖环境准备、依赖安装、配置文件、API/界面启动、常见验证方式
- 技术栈要说明各组件职责，例如 `Gradio`、`FastAPI`、`SQLite`、`ChromaDB`

4. 校验
- 修改后执行幂等校验，优先运行与项目结构相关的测试
- 至少确认 README 与版本修改未引入明显结构问题

5. Git 操作
- 完成代码与文档修改后整理变更摘要
- `git commit` 与 `git push` 必须在用户再次确认后执行

## 本次执行顺序

1. 更新版本到 `0.5`
2. 重写并完善 `"readme.md"`
3. 执行允许范围内的校验
4. 汇报变更内容
5. 等待你确认后再执行 `git commit`
6. 再等待你确认后执行 `git push`


## 第十阶段

1. 知识库模型
- 新增 知识库 实体，支持唯一 ID、名称、说明、状态、创建时间、更新时间
- 文档归属于指定知识库，数据库中的文档主记录需要增加知识库归属字段
- 所有知识库操作保持异常处理，数据库写操作统一走事务

2. 目录结构调整
- Input 目录改为二级目录结构：Input/<知识库标识>/
- 文档扫描、注册、批量注册、重建都需要基于当前选中的知识库目录
- 兼容已有单层 Input 数据的迁移策略需要明确，至少保证不崩溃

3. 设置页知识库管理
- 在 功能设置 中增加知识库增删改模块
- 支持新增知识库、编辑名称/说明、删除知识库
- 删除前需要校验是否存在归属文档，并给出明确提示

4. 文档管理页改造
- 知识库管理 页顶部增加当前知识库选择器
- 当前页面只展示所选知识库下的输入文档、入库状态、重建项与入库质检结果
- 文档注册时自动归属到当前知识库

5. 检索与 AI 质检改造
- 知识库检索 执行前必须先选定知识库
- AI 质检 执行前必须先选定知识库
- 检索、证据召回、质检历史读取都限制在当前知识库范围内

6. 数据访问与服务层
- repository / service / viewmodel / ui 全链路增加知识库上下文
- 文档列表、状态统计、检索、质检历史、人工审核候选等查询都要评估是否需要按知识库过滤
- 尽量避免一次修改散落过多位置，优先提取公共知识库上下文参数

7. 测试与迁移
- 先补数据模型与 repository/service 单测
- 再补 viewmodel/UI 结构测试
- 最后做真实页面验收
- 若涉及历史库结构升级，需要补初始化/迁移测试

8. 验收标准
- 能在设置页管理多个知识库
- 能在页面顶部切换当前知识库
- 不同知识库下的文档、检索、AI 质检结果互相隔离
- 未选择知识库时，相关操作给出明确提示而不是静默失败

## 第十阶段进度

- 已完成：数据库结构扩展，新增 `knowledge_bases` 表
- 已完成：历史库迁移补齐 `documents.knowledge_base_id` 与 `quality_checks.knowledge_base_id`
- 已完成：默认知识库自动创建与历史数据回填
- 已完成：新增 `KnowledgeBaseService`，支持知识库查询、保存、删除前校验与目录创建
- 已完成：检索、质检、审核 repository/service 初步支持按知识库过滤
- 已完成：`scan_input_documents(...)` 支持按 `Input/<知识库标识>/` 扫描
- 已完成：新增后端单测 `tests/unit/test_knowledge_base.py`，当前 `3 passed`
- 已完成：`src/ui/pages.py` 的多知识库 UI 接线，新增 `"知识库管理"`、`"知识库检索"`、`"AI 质检"`、`"人工审核"` 顶部知识库选择器
- 已完成：`"功能设置"` 新增知识库 CRUD 工作区，支持列表分页、详情、表单、新建、刷新、保存、删除
- 已完成：文档管理、检索、AI 质检、人工审核、效果评测、批量入库质检与相关导出操作透传当前知识库
- 已完成：补充 `tests/unit/test_ui.py` 结构断言并执行 `tests/unit/test_ui.py`、`tests/unit/test_knowledge_base.py`，当前 `26 passed`
- 已完成：修复旧 SQLite 库启动时报 `no such column: knowledge_base_id` 的初始化顺序问题，补充旧库迁移回归测试，当前 `tests/unit/test_knowledge_base.py` 为 `4 passed`
- 已完成：临时端口 `7865` 浏览器验收，确认 `"AI 质检"`、`"人工审核"`、`"知识库管理"`、`"知识库检索"` 均已显示 `"当前知识库"` 选择器，且 `"功能设置"` 已显示知识库 CRUD 区
- 已完成：定位并修复 `"功能设置"` 中知识库列表只渲染首条的问题，将知识库列表从 `Dataframe` 改为稳定的 `Radio + 分页` 选择器
- 已完成：执行 `python -m pytest "tests/unit/test_ui.py" "tests/unit/test_knowledge_base.py" -q`，当前 `28 passed`
- 已完成：在 `7860` 新开页签做浏览器验收，确认 `"功能设置"` 页知识库列表已真实显示 `default` 与 `kb_acceptance_7860` 两条记录
- 已完成：补充多知识库隔离后端回归测试，覆盖文档状态、数据库摘要、人工审核候选、人工审核历史按知识库过滤；执行 `python -m pytest "tests/unit/test_knowledge_base.py" -q`，当前 `8 passed`
- 已完成：修复 `kb_acceptance_7860` 最小验收样本的中文乱码，重写 `"Input/kb_acceptance_7860/acceptance_note.MD"` 与对应 `documents / document_sections / chunks / chunk_fts / quality_checks / quality_claims` 记录
- 已完成：运行时只读核对通过，`kb_acceptance_7860` 当前稳定为 `1` 条文档、`1` 条质检、`1` 条待审核 Claim、`0` 条审核历史，检索词 `"东阿"` 可命中 `kbacc_chunk_1`
- 下一步：继续做真实交互验收，重点验证顶部 `"当前知识库"` 下拉的跨页面同步切换，以及浏览器页面中的最终展示是否与运行时核对结果一致
