# UI 按钮规范整改 Todo

> 时间：2026-05-09
> 范围："src/ui/pages.py" 全页面按钮样式统一
> 原则：先定按钮规范，再替换页面实现；删除零散补丁式按钮 CSS

## 目标

1. 提炼统一按钮规范，参考 "AI 质检" 页当前可接受按钮样式
2. 建立按钮语义类：主按钮、次按钮、危险按钮、分页/轻操作按钮
3. 页面按钮全部挂语义类，不再靠页面容器大范围强覆盖
4. 清理冗余按钮规则，保留必要布局规则

## 执行步骤

1. 在 "UI_CSS" 中新增按钮 token 与语义类
2. 批量给所有 "gr.Button" 增加对应 "elem_classes"
3. 删除/收敛零散的页面级按钮视觉规则，只保留布局规则
4. 运行诊断、py_compile、UI 单测

## 验收标准

- 所有页面按钮视觉语言统一
- "AI 质检" 页按钮样式保持当前可接受效果
- primary / secondary / danger / pagination 层级清晰
- 不依赖最终强覆盖或超大容器选择器
- 不新增冗余嵌套与重复 CSS