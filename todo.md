# Todo

## 当前任务

- 将 `"AI 质检优化 Dummy"` 的结构迁移到正式 `"AI 质检"` 页面，在不破坏现有业务逻辑的前提下完成正式页改造。

## 设计规范

### 1. 页面目标

- dummy 不是装饰稿，必须能完整表达未来正式页的主要功能与操作路径。
- 用户进入页面后，必须能看懂：输入什么、系统当前在处理什么、结果在哪里看、下一步能做什么。
- 禁止出现“只有标题没有实际内容”的占位区。

### 2. 布局规范

- 一行最多左右两列，禁止三列并排。
- 优先使用单层主区块，禁止为视觉效果堆叠多层 `Group / Row / Column`。
- 页面主线按“输入与执行 -> Claim 与详情 -> 证据 -> 历史与动作”展开。
- 同一层只放同一类信息，避免说明、结果、操作按钮混在同一块。

### 3. 功能完整性规范

- 必须保留输入区：待质检文本、知识库、模板、执行按钮。
- 必须保留执行反馈：当前状态、总体结论、风险等级、Claim 数量。
- 必须保留 Claim 工作区：Claim 列表、当前 Claim 详情、建议动作。
- 必须保留证据工作区：证据列表、证据详情、证据关系说明。
- 必须保留历史工作区：最近质检记录、当前选中记录说明。
- 必须保留动作结果区：导出、送审、生成复核任务等真实可见内容，不能只写一句抽象说明。

### 4. 组件规范

- 表格只承载列表，不承载大段说明文本。
- 说明文字尽量并入对应功能区，不单独拆出冗余卡片。
- 一个区域最多一个主标题；副说明紧跟标题，不再额外套标题卡。
- 优先让详情区直接展示有效内容，不做“说明卡 + 内容卡”双层结构。
- 详细功能说明统一放到页面最下面，避免打断主操作路径。

### 5. 视觉规范

- 卡片数量从少，不靠多层边框制造层次。
- 同级区域使用统一边框、圆角、内边距。
- 强调信息只保留一种高亮方式，避免每块都像重点。
- 视觉层级依赖标题、留白、轻边框，不依赖复杂嵌套。

## 正式页改造方案

### 方案骨架

1. 第一行：左右两列
- 左侧：输入与执行
- 右侧：执行状态与总体结果

2. 第二行：左右两列
- 左侧：Claim 列表
- 右侧：当前 Claim 详情

3. 第三行：左右两列
- 左侧：证据列表
- 右侧：证据详情

4. 第四行：左右两列
- 左侧：历史质检记录
- 右侧：后续动作结果区

5. 最底部：单列
- 详细功能说明

### 后续动作结果区内容

- 导出结果示例
- 提交人工审核入口示意
- 生成复核任务示意
- 当前选中记录摘要
- 操作后反馈示例

## 执行步骤

1. 重排正式 `"AI 质检"` 页布局，改成四行双列加底部单列说明。
2. 减少正式页中的多层 `Group / Row / Column` 嵌套。
3. 保留现有功能与事件链，只调整展示结构和组件归位。
4. 将详细功能说明统一下沉到页面最底部。
5. 补齐“动作结果区”的展示承接，不让按钮和结果分散。
6. 更新 `"tests/unit/test_ui.py"`，校验正式页新结构。
7. 跑 UI 聚焦测试与诊断。

## 验收标准

- 正式 `"AI 质检"` 页面没有三列布局。
- 页面层级明显减少，结构更接近 dummy 定稿。
- 现有质检、Claim、证据、历史、评测、导出逻辑保持可用。
- 详细功能说明位于页面最底部。
- `tests/unit/test_ui.py` 与诊断检查通过。
