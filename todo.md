# TODO

## 2026-04-30

- [x] 评审现有方案中的过度设计点，确认需要收敛的能力边界
- [x] 输出一版独立的 MVP 实施方案文档，保留原始方案不覆盖
- [x] 补充评测指标、降级策略、风险控制和实施路径
- [x] 更新 `chat_history.md` 记录上一轮对话与处理结果
- [x] 检查新增文档是否存在格式或诊断问题
- [x] 输出一版仅覆盖 Markdown 入库之后功能的 `tasks.MD`
- [x] 将本轮“跳过 PDF 处理、直接从 MD 入库开始”的要求写入 `chat_history.md`
- [x] 检查新增任务文档是否存在格式或诊断问题
- [x] 输出 `Docs/design.MD`，细化模块边界、数据表、核心流程与异常策略
- [x] 将本轮“继续 design”的要求写入 `chat_history.md`
- [x] 检查 `design.MD` 是否存在格式或诊断问题
- [x] 将整体方案从“医学古籍专用”调整为“通用中文知识库，兼容少量古文与医学内容”
- [x] 更新 `chat_history.md` 记录本轮范围调整
- [x] 检查方案文档、tasks 文档、design 文档是否存在领域表述残留或诊断问题
- [x] 将 `Docs` 下的方案文件名统一改为“中文知识库系统”风格
- [x] 输出 `Docs/api.MD`，补充接口定义、请求响应与错误码
- [x] 更新 `chat_history.md` 记录本轮重命名和 API 文档工作
- [x] 检查 `api.MD` 与重命名后的文档是否存在诊断问题
- [x] 搭建项目代码骨架，覆盖 `common`、`db`、`app`、`tests` 基础目录
- [x] 新增依赖清单与基础配置文件，支持本地开发与测试环境
- [x] 实现 SQLite 初始化、事务封装与基础数据表
- [x] 实现最小 `FastAPI` 应用与健康检查接口
- [x] 实现文档注册与状态查询的最小 API 闭环
- [x] 新增最小测试骨架并执行校验
- [x] 更新 `chat_history.md` 记录本轮代码搭建过程
- [x] 接入真实向量索引层，替换占位 `vector_search`
- [x] 实现向量写入流程，并在文档入库时同步建立向量索引
- [x] 补充混合检索测试，覆盖全文检索与向量检索融合
- [x] 为下一阶段改动追加 `chat_history.md`
- [x] 细化 Markdown 章节抽取，完善 `document_sections` 与 `chunks` 之间的关联
- [x] 补充文档重建逻辑，使 `rebuild` 真正触发 FTS 与向量重建
- [x] 设计并实现第一版规则引擎骨架
- [x] 区分 `rebuild_fulltext` 与 `rebuild_vector` 的重建行为
- [x] 搭建最小 UI 页面：文档管理、检索、质检、审核
- [x] 为本轮提交后的继续开发追加 `chat_history.md`
- [x] 将 Gradio UI 与启动入口真正接起来
- [x] 细化规则等级、命中原因与 verdict 映射
- [x] 为 UI 和规则细化补充测试
- [x] 将 UI 页面接入真实文档注册入口与结果刷新
- [x] 为 claim 与 review 增加更友好的展示字段
- [x] 整理 README/启动说明，补充 FastAPI 与 Gradio 启动方式
- [x] 为 UI 增加文档重建入口与状态联动刷新
- [x] 为审核页增加最近 claim/最近质检结果联动
- [x] 继续收敛开发文档与 README 的一致性
- [x] 为 UI 增加文档注册后的自动状态刷新
- [x] 为检索结果增加来源文档标题与更友好的展示结构
- [x] 为 JSON 输入增加最小兼容读取、元数据提取与校验错误处理
- [x] 扩展 JSON 专用元数据映射（作者、来源、标签等）
- [x] 为旧版 SQLite `documents` 表增加幂等补列迁移
- [x] 修复质检 `doc_uid` 证据范围收敛
- [x] 为 `quality_claims` 增加 `risk_level` 持久化与兼容迁移
- [x] 为质检接口增加 `2000` 字输入限制
- [x] 将 LLM 做成 `.env` 可配置 API，兼容 `openai` 与 `anthropic`
- [x] 为 Embedding 增加 provider 抽象，支持 `local` 回退与 `openai` 在线模式
- [x] 将质检流程接入可注入 LLM 客户端并保留启发式回退
- [x] 增加模型连通性自检模块与 `.aipython` 一键脚本
- [x] 将质检 Prompt 做成可切换模板，并提供内置预设模板
- [x] 让质检模板联动规则标签与检索策略，并补齐模板 YAML 预设文件
- [x] 为模板策略化补充回归测试并同步 README / API 文档
- [x] 扩展 UI 检索结果展示更多文档元信息（author/source_name/tags）
- [x] 为混合检索接入可配置 rerank 客户端，并支持 `dashscope/openai` 两类协议
- [x] 为 `/search/hybrid` 接入 `use_rerank` 开关，并在结果中补充 `matched_sources/rerank_score`
- [x] 为 rerank 增加单元测试、文档同步与全量回归验证
- [x] 为模型自检脚本增加 `rerank` 连通性检查
- [x] 优化 UI 检索结果视图，展示 `matched_sources/rerank_score`
- [x] 让质检模板支持配置 `use_rerank`
- [x] 为质检结果补充证据级 `matched_sources/rerank_score/context_mode` 解释字段
- [x] 为审核页增加当前 Claim 证据详情联动展示
- [x] 优化审核页展示结构，拆分为 claim 摘要区和证据表
- [x] 审核提交后自动刷新 claim 状态、最近质检结果与最近审核记录
- [x] 支持从最近审核记录定位回对应 claim
- [x] 修复 Embedding 批量入库时 400 导致的向量索引写入失败
- [x] 修复文档入库状态与向量库实际状态不一致的问题
- [x] 为文档管理页增加现有文档列表、文档详情与单文档操作状态
- [x] 将文档管理页重构为左右双栏并补充功能说明
- [x] 为注册与重建流程补充更细粒度的阶段进度和总体进度展示
- [x] 增强 Embedding 超时重试、递归拆批与连接复用，提升大文档向量写入稳定性
- [x] 完成最新全量回归测试并核对 SQLite / Chroma 真实入库结果
