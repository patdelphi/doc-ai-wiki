# Todo

## 当前任务

- 分支目标：在 `dev` 分支推进 UI 优化，不直接修改 `main`
- 当前阶段：设计文档已完成，进入逐模块实施阶段
- 设计依据：遵循 `frontend-design` skill，但按当前项目约束收敛为“冷静、专业、轻编辑化”的知识工作台风格

## 执行计划

1. 先整理全局布局规则与页面共性样式
2. 先做 `AI 质检优化 Dummy`，用于确认新版设计方向
3. Dummy 确认后，再优化 `AI 质检` 正式页
4. 然后优化 `人工审核`，对齐列表、详情、动作闭环
5. 再优化 `知识库管理`，压缩冗余摘要与操作分散问题
6. 再优化 `知识库检索`，统一输入、结果、详情、导出结构
7. 最后优化 `功能设置`，统一配置页的信息分组与动作区
8. 每完成一个模块，补对应 UI 测试并执行聚焦校验
9. 最后做全局 CSS 清理与验收回归

## 模块顺序与输出物

### 第一阶段：设计与基线

- 已完成 `Docs/ui_redesign_design.MD`
- 已收敛全局 UI 规范
- 当前开始落地页面骨架与样式统一

### 第二阶段：逐模块优化

- `AI 质检优化 Dummy`（当前进行中）
- `AI 质检`
- `人工审核`
- `知识库管理`
- `知识库检索`
- `功能设置`

### 第三阶段：收尾

- 清理冗余 CSS
- 核对 `elem_id` 稳定性
- 跑 `test_ui.py` 与诊断检查
- 视情况补最小必要回归测试

## 验收标准

- 一行最多左右两列
- 不再出现明显多层嵌套卡片
- 动作和结果在同一区域闭环展示
- 列表与详情职责清晰分离
- 各页面视觉语言统一
- `dev` 分支持续可验证

## 当前决定

- 先按文档逐页推进
- 不再停留在计划阶段，直接进入实现
- 当前先用 `Gradio` 输出 dummy 页给你确认方向
- 每完成一个模块后只做简短汇报，然后继续下一个模块
