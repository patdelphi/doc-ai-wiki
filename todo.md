# Todo

## 当前高优先级任务

- 提升 `"AI 质检"` 的结果持久化可靠性，避免页面显示成功但刷新后历史记录缺失。
- 先区分“服务流式链路异常”和“数据库写入异常”，确保成功态必须以真实落库为准。
- 为 `"AI 质检"` 增加写库后的回读校验与明确提示，确保刷新后可在历史与人工审核中找到记录。

## 本次执行步骤

1. 复现 `"AI 质检"` 成功显示但未进入历史记录的问题。
2. 检查 `quality_checks`、`quality_claims` 的写库与回读链路是否存在假成功。
3. 修复成功态判断逻辑，只在回读校验通过后展示最终成功结果。
4. 补充单元测试，覆盖“写库后回读成功”和“回读失败不得返回成功态”。

## 当前阶段

- 基于已完成的 `"AI 质检"` 页面与下载渲染优化，推进其他页面 UI 与下载渲染内容的一致化改造。

## 已完成基线

- `"AI 质检"` 正式页已完成主布局优化。
- 下载结果区已调整为按钮在上、结果详情在下，并支持长 URL 折行。
- AI 质检导出预览已优化宽表、证据详情样式与 Claim 顺序。
- `"AI 质检优化 Dummy"` 菜单将隐藏，不再作为主导航入口。

## 下一步目标

### 1. 优化其他页面 UI

- 对齐 `"人工审核"` 页面布局，减少说明卡与内容卡重复堆叠。
- 对齐 `"知识库管理"` 页面布局，收紧顶部操作区、详情区与结果区层级。
- 对齐 `"知识库检索"` 页面布局，统一结果列表、详情区、导出动作区的视觉结构。
- 对齐 `"功能设置"` 页面布局，统一表单区、说明区、表格区的间距与卡片风格。

### 2. 优化其他下载渲染内容

- 将 `"人工审核"` 导出内容改成与 AI 质检一致的“摘要 -> 主体详情 -> 逐项明细”结构。
- 将 `"知识库检索"` 导出内容改成窄屏友好的分块文本，避免宽表横向滚动。
- 将 `"文档管理"` 的导出结果卡与预览页样式统一到现有下载结果模板。
- 统一所有导出预览 HTML 的长文本换行、表格固定布局、链接折行策略。

## 页面 UI 统一规范

- 一行最多左右两列，避免三列及以上并排。
- 说明文字优先并入对应功能区，不额外拆冗余说明卡。
- 列表组件只承载列表，不承载大段正文。
- 详情区优先展示有效内容，不做“说明卡 + 内容卡”双层重复结构。
- 动作按钮与动作结果必须放在同一功能区内，避免分散。

## 下载渲染统一规范

- 导出正文优先使用“摘要 + 分块明细”，谨慎使用宽表。
- 长 URL、长文件名、长正文必须强制折行。
- 证据详情、原文片段、备注说明统一使用普通文本块，不使用易横向滚动的引用样式。
- 同一类导出页面统一标题层级、卡片样式、表格换行规则。

## 建议执行顺序

1. 先做 `"人工审核"` 页面 UI 与导出渲染统一。
2. 再做 `"知识库检索"` 页面 UI 与导出渲染统一。
3. 然后做 `"文档管理"` 各类导出结果与预览页统一。
4. 最后统一 `"功能设置"` 页面布局细节与帮助区样式。
5. 每完成一个页面，都补对应测试并跑聚焦校验。

## 验收标准

- 主导航只保留正式可用页面，不显示 Dummy 菜单。
- 其他页面的布局风格与 `"AI 质检"` 保持同一套视觉规则。
- 所有下载结果区都支持按钮上置、详情下置、长链接折行。
- 所有导出预览页都避免明显横向滚动和重复信息堆叠。
- 对应 `tests/unit/*` 聚焦测试与诊断检查通过。
