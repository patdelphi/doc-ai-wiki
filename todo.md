# UI 整体整改 Todo

> 时间：2026-05-09
> 依据文档："Docs/ui_redesign_design.MD"
> 原则：先回退错误改动，再按规范做整体重排；不继续打局部补丁。

## 本轮目标

1. 先把 "知识库检索" 页的 "下载结果" 按钮恢复到原先语义位置，不再挂在摘要区右上角
2. 对当前 UI 做整体整改，不再头疼医头脚痛医脚
3. 所有整改都必须服从设计规范，不改业务逻辑、不改事件链路

## 发现的问题

### 1. 知识库检索

- "下载结果" 按钮位置被改错，当前挂在摘要区右侧，不符合“动作与结果同区闭环”
- 结果区层级仍不够稳定，存在结果工作区包裹过重的问题
- 结果表格、详情、下载反馈的语义分区还不够清楚

### 2. 知识库管理

- 顶部说明、选择器、摘要、当前文档区的容器层级偏多
- "当前选中文档" 已经是主卡片，但内部详情还有二次包裹感
- 主操作与主对象之间距离偏远，主路径不够短

### 3. 功能设置

- 页面仍偏“模块堆叠”，没有完全做到“列表优先、详情/编辑其次、结果下沉”
- 导出动作区和主编辑区关系还可以继续收敛

### 4. AI 质检

- 主流程顺序基本对，但局部帮助区、结果区、评测区还需要再降噪
- 某些表格和说明块还存在并排过满的问题

### 5. 人工审核

- 候选列表、证据列表、详情、动作之间的主从关系还不够稳定
- 需要再核对是否真正满足“宽列表优先、结果贴近动作”

## 执行顺序

### 第一阶段：错误回退

1. 把 "知识库检索" -> "下载结果" 按钮改回你要求的位置
2. 清理这次错误引入的搜索页补丁样式，避免继续污染结构

### 第二阶段：整体设计 Review 落地

1. 逐页对照 "Docs/ui_redesign_design.MD" 做结构检查
2. 按“一个语义模块一个主外框”收敛容器层级
3. 按“表格单栏优先、必要时双栏”重排页面主工作区
4. 按“动作和结果闭环”重新放置按钮、结果区、导出区

### 第三阶段：按页整改

1. "知识库检索"
   - 顶部输入 / 说明
   - 结果表格
   - 当前详情
   - 下载动作 + 下载结果闭环
2. "知识库管理"
   - 当前文档优先
   - 操作前置
   - 文档表格全宽优先
   - 入库质检内部继续去嵌套
3. "功能设置"
   - 列表优先
   - 详情与编辑合理并排
   - 页底结果与导出独立收尾
4. "AI 质检"
   - 保留主流程
   - 非核心区降级
   - 宽表格优先
5. "人工审核"
   - 宽列表优先
   - 详情/动作按高度决定是否并排
   - 结果贴近动作

### 第四阶段：验证

1. 检查 "src/ui/pages.py" 诊断
2. 更新必要测试
3. 运行幂等校验：	est / py_compile
4. 最后做一次逐页人工 review

## 验收标准

- 不再出现三层及以上视觉外框嵌套
- 不再靠加边框掩盖结构问题
- 表格类模块默认全宽单栏
- 按钮都在所属模块内，且与结果闭环
- 页面主路径清晰，帮助信息不抢主流程
- 保持 "Gradio" 与现有业务逻辑不变

## 待你确认

- 是否按这份 "todo.md" 执行？
- 如果执行，我先做第一阶段：把 "知识库检索" 的 "下载结果" 按钮恢复原位，再开始整体整改。