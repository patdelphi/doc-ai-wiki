# Todo

## 目标

- 重新设计 `"AI 质检"` 页面排版，不再在当前布局上局部修补
- 让信息分区清晰、视觉密度合理、主次明确，避免现在“表格和卡片四处分散”的感觉
- 保留已有功能，但按更符合直觉的阅读顺序重新组织

## 设计方向

- 顶部只保留一个主操作区：左侧输入，右侧功能说明
- 模板展示区单独占一整行，完整展示 `"模板内容"`、`"系统提示词"`、`"用户提示模板"`
- 结果区按“先总览，再明细”的顺序组织，不再把历史、证据、详情交叉摆放
- 底部减少并列模块数量，尽量控制为每行 2 个主区域

## 建议新布局

1. 第一行：输入区 + 功能说明
- 左侧：`"待质检文本"`、`"质检模板"`、`"开始质检"`、`"加载最近质检结果"`
- 右侧：功能说明与当前操作提示

2. 第二行：模板展示区
- 整行展示 `"模板内容"`、`"系统提示词"`、`"用户提示模板"`
- 三块并列，大屏下等高；必要时允许内部滚动，不压缩到右侧窄栏

3. 第三行：执行进度 + 结果摘要
- 左侧：执行进度
- 右侧：总体质检结果

4. 第四行：Claim 列表 + Claim 详情
- 左侧宽表格：Claim 列表
- 右侧详情卡：当前选中 Claim 的结论、风险等级、来源、说明

5. 第五行：证据区
- 左侧：证据列表
- 右侧：证据详情
- 点击证据列表后，右侧刷新对应证据详情

6. 第六行：最近质检记录
- 单独整行或单独模块，不和证据区混排
- 明确标注这是历史记录入口

## 待办

1. 重构 `"AI 质检"` 页面结构
- 调整 `"src/ui/pages.py"` 中的行列组织
- 删除当前造成视觉混乱的多处并列块

2. 重构模板展示区
- 调整 `"src/ui/viewmodels.py"` 中模板 HTML
- 确保三块模板信息是真正的整行展示

3. 收口结果区交互
- 保留 `Claim -> 详情`
- 保留 `证据 -> 详情`
- 历史记录只负责回看历史，不混入当前证据阅读区

4. 统一文案与层级
- 风险等级继续保持中文
- `"最近质检结果"` 改成更直观的历史说明

5. 补测试与浏览器验收
- 更新 UI 结构测试
- 更新 Claim / 证据点击联动测试
- 用浏览器重新验收新版排版

## 备注

- git commit / push 按你的规则需要再次明确确认后才执行
