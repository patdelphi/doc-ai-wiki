# Todo

## 当前提交

- 提交当前人工审核工作区、筛选修复、排序修复、相关测试与文档更新
- 提交时排除 `"__pycache__"`、`".trae_ui_*.log"` 等生成文件

## 新需求

- 在系统菜单中增加 `"功能设置"` 模块
- 支持质检模板的新增、编辑、删除
- 补充其他需要配置的系统项，尽量让非代码方式可维护

## 设置模块范围

1. 模板管理
- 列出现有模板
- 查看模板详情
- 新增模板
- 编辑模板
- 删除模板
- 模板字段校验与异常提示

2. 检索配置
- 默认 `top_k`
- 是否启用重排
- 默认重排策略
- 质检检索候选数默认值

3. 质检配置
- 默认模板
- 最大输入长度
- 审核候选抓取上限
- 结果展示相关开关

4. 运行配置
- LLM / Embedding / Rerank 当前生效配置只读展示
- 必要时增加“从配置文件重新加载”入口

## 实现步骤

1. 数据模型与存储
- 梳理模板当前来源：内置模板 + `"templates/quality"` 文件
- 设计用户模板的读写方式，优先复用 YAML 文件
- 明确内置模板与用户模板的覆盖规则

2. 服务层
- 增加模板的增删改查服务
- 增加配置读取与校验服务
- 所有接口补异常处理

3. UI
- 在主界面增加 `"功能设置"` 标签页
- 左侧做设置导航或列表，右侧做详情与编辑区
- 模板编辑区支持保存、删除、新建

4. 测试
- 先补模板 CRUD 测试
- 再补设置页视图与交互测试
- 最后补最小集成校验

5. 验收
- 浏览器验证模板新增、编辑、删除
- 验证修改后质检页可读取最新模板

## 备注

- 所有新增配置优先走现有文件配置体系，避免引入不必要的数据库表
- 如果设置项超出本轮范围，先做只读展示，后续再补可编辑能力
