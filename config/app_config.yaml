# 历史样例：当前运行时不会直接读取本文件。
# 实际主配置来源是 ".env" / 环境变量，经 `src/common/config.py` 的 `AppSettings` 加载。
default_page: 1
default_page_size: 20
default_top_k: 5
max_input_length: 4000
chunk_size: 500
chunk_overlap: 100
enable_cache: false
quality_template: "default"
retry_count: 1
input_root: "Input"
