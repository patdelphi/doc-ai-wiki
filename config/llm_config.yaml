# 历史样例：当前运行时不会直接读取本文件。
# 实际模型配置来源是 ".env" / 环境变量，经 `src/common/config.py` 的 `AppSettings` 加载。
primary_provider: "qwen"
primary_model: "qwen-max"
fallback_provider: ""
fallback_model: ""
embedding_provider: "dashscope"
embedding_model: "text-embedding-v3"
rerank_provider: "dashscope"
rerank_model: "default"
timeout_seconds: 60
cost_threshold: 5.0
