# 代码审查整改清单

## 说明
- 基于本次项目静态审查与最小测试验证整理。
- 本文档只记录整改清单，不修改现有 todo。
- 优先级分为：P0、P1、P2。

## P0 立即处理
- [ ] 为 "src/app.py" 的写接口与管理接口补齐认证与鉴权。
- [ ] 明确 "src/app.py" 的读取接口是否也需要登录后访问，并统一访问策略。
- [ ] 修复 "src/auth/service.py" 中默认创建 "admin/admin" 的逻辑，禁止弱默认口令。
- [ ] 修复 "src/auth/service.py" 中 "authenticate()" 未校验 "is_active" 的问题。
- [ ] 让 "src/ui/pages.py" 真正消费 "tab_names" 与 "kb_ids"，按权限裁剪 Tab 和知识库范围。
- [ ] 修复已失效的启动脚本测试，统一脚本命名与测试断言。

## P1 高优先级
- [ ] 调整启动顺序：先初始化 SQLite，再构造 "VectorStore"，避免启动路径顺序依赖。
- [ ] 为 "src/db/schema.py" 中 "review_records.claim_id" 增加外键或等效一致性约束。
- [ ] 为删除知识库流程补充 "quality_checks" 等关联数据校验，避免只检查文档数量。
- [ ] 收敛认证初始化职责，避免 DB 初始化与业务层同时补认证数据。
- [ ] 评估并修复 "src/ui/app.py" 中长生命周期 SQLite 连接的并发与关闭问题。
- [ ] 为认证模块补最小单测：登录、禁用用户、注册、改密、权限更新。

## P2 结构优化
- [ ] 拆分 "src/ui/pages.py"，按页面布局、事件绑定、权限控制分层。
- [ ] 拆分 "src/db/repositories.py"，避免单仓储文件持续膨胀。
- [ ] 清理 "Docs/migrations" 下重复或一次性认证脚本，避免误用。
- [ ] 复核 UI 首屏显示逻辑，避免依赖 BrowserState 恢复后才显示页面。
- [ ] 为知识库删除、审核记录孤儿数据、旧库升级路径补充回归测试。

## 已确认问题依据
- [ ] "tests/unit/test_startup_scripts.py" 当前会因缺少 "start_gradio.ps1" 与 "start_gradio.sh" 失败。
- [ ] "tests/integration/test_app.py" 中启动脚本相关用例当前会失败。
- [ ] "src/app.py" 中接口未见认证依赖或权限校验。
- [ ] "src/ui/pages.py" 中权限写入登录态，但未见主界面按权限裁剪。
- [ ] "src/auth/service.py" 中存在默认管理员初始化与弱口令风险。

## 建议验收标准
- [ ] 未登录用户不能访问受保护接口与受保护页面。
- [ ] 被禁用用户无法登录，普通用户只能看到被授权的 Tab 与知识库。
- [ ] 不再存在默认弱口令管理员；初始化流程无隐式高危副作用。
- [ ] 启动脚本命名、文档、测试三者一致。
- [ ] 知识库删除、审核记录、旧库兼容路径均有自动化测试覆盖。
- [ ] 相关测试通过，至少包含 "lint"、"test"、必要的 "build" 或 "type-check"。

## 建议执行顺序
1. 先修权限边界与默认管理员问题。
2. 再修启动脚本测试与启动顺序问题。
3. 然后补数据库一致性与认证测试。
4. 最后做 UI 与 Repository 结构拆分。